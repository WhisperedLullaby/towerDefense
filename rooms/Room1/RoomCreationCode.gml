// Set window size to desktop size for borderless windowed mode
//var desktopWidth = display_get_width();
//var desktopHeight = display_get_height();
//window_set_size(desktopWidth, desktopHeight);

// Make the window borderless
//window_set_showborder(true)

// Adjust the viewport and camera as per aspect ratio (similar to the fullscreen code)
//var newWidth, newHeight;
//if (desktopWidth / desktopHeight > 16/9) {
//   newHeight = desktopHeight;
//   newWidth = newHeight * (16/9);
//} else {
//    newWidth = desktopWidth;
//    newHeight = newWidth / (16/9);
//}

//var viewX = (desktopWidth - newWidth) / 2;
//var viewY = (desktopHeight - newHeight) / 2;
